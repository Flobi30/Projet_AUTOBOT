<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AUTOBOT - Login</title>
    <style>
        :root {
            --primary-color: #00ff9d;
            --bg-color: #121212;
            --card-bg-color: #1e1e1e;
            --text-color: #ffffff;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        
        .login-container {
            background-color: var(--card-bg-color);
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 255, 157, 0.2);
            padding: 40px;
            width: 100%;
            max-width: 400px;
            text-align: center;
        }
        
        .logo {
            color: var(--primary-color);
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 10px;
            text-shadow: 0 0 10px rgba(0, 255, 157, 0.5);
        }
        
        .subtitle {
            color: var(--text-color);
            margin-bottom: 30px;
            font-size: 0.9rem;
        }
        
        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }
        
        label {
            display: block;
            margin-bottom: 5px;
            color: var(--primary-color);
        }
        
        .input-container {
            position: relative;
        }
        
        input {
            width: 100%;
            padding: 12px;
            border: 1px solid #333;
            background-color: #222;
            color: white;
            border-radius: 5px;
            box-sizing: border-box;
        }
        
        input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 5px rgba(0, 255, 157, 0.5);
        }
        
        .toggle-password {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: #777;
            cursor: pointer;
        }
        
        .btn {
            background-color: var(--primary-color);
            color: #121212;
            border: none;
            padding: 12px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            width: 100%;
            margin-top: 10px;
            transition: all 0.3s;
        }
        
        .btn:hover {
            background-color: #00cc7d;
            box-shadow: 0 0 10px rgba(0, 255, 157, 0.7);
        }
        
        .error-message {
            color: #e74c3c;
            margin-top: 15px;
            font-size: 0.9rem;
        }
        
        .alt-login-buttons {
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        
        .btn-direct {
            background-color: #3498db;
        }
        
        .btn-simple {
            background-color: #e74c3c;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="logo">AUTOBOT</div>
        <div class="subtitle">Trading and Automation Framework</div>
        
        <form id="loginForm" method="post" action="/login">
            <div class="form-group">
                <label for="username">Nom d'utilisateur</label>
                <div class="input-container">
                    <input type="text" id="username" name="username" placeholder="Entrez votre nom d'utilisateur" required>
                </div>
            </div>
            
            <div class="form-group">
                <label for="password">Mot de passe</label>
                <div class="input-container">
                    <input type="password" id="password" name="password" placeholder="Entrez votre mot de passe" required>
                    <button type="button" class="toggle-password">👁️</button>
                </div>
            </div>
            
            <div class="form-group">
                <label for="license_key">Clé de licence</label>
                <div class="input-container">
                    <input type="text" id="license_key" name="license_key" placeholder="Entrez votre clé de licence" required>
                    <button type="button" class="toggle-password">👁️</button>
                </div>
            </div>
            
            <button type="submit" class="btn">SE CONNECTER</button>
            
            <div class="alt-login-buttons">
                <a href="/direct-login" class="btn btn-direct">⚡ Connexion Directe</a>
                <button type="button" id="simpleLoginBtn" class="btn btn-simple">🔒 Connexion Simple</button>
            </div>
            
            {% if error %}
            <div class="error-message">{{ error }}</div>
            {% endif %}
        </form>
    </div>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Toggle password visibility
            const toggleButtons = document.querySelectorAll('.toggle-password');
            toggleButtons.forEach(button => {
                button.addEventListener('click', function() {
                    const input = this.previousElementSibling;
                    if (input.type === 'password') {
                        input.type = 'text';
                        this.textContent = '🔒';
                    } else {
                        input.type = 'password';
                        this.textContent = '👁️';
                    }
                });
            });
            
            // Handle simple login button
            const simpleLoginBtn = document.getElementById('simpleLoginBtn');
            const loginForm = document.getElementById('loginForm');
            
            if (simpleLoginBtn) {
                simpleLoginBtn.addEventListener('click', function(e) {
                    e.preventDefault();
                    loginForm.submit();
                });
            }
        });
    </script>
</body>
</html>
