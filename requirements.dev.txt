# Development dependencies
pytest==7.4.0
pytest-cov==4.1.0
vulture==2.7
radon==6.0.1
responses==0.23.1
# These are already in the main requirements.txt, no need to duplicate
# python-dotenv
# pandas
# fastapi
# uvicorn
